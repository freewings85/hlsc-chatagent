将用户说的关键词匹配为系统中的养车服务项目，返回 project_id 和项目名称。

当用户提到服务项目名称（如"洗车"、"四轮定位"、"保养"）时，调用此工具。需要 car_model_id 参数用于按车型过滤结果。

返回结果分为精确匹配和模糊匹配：
- 精确匹配 1 个：直接使用该 project_id
- 精确匹配多个或只有模糊匹配：告知用户匹配结果，让用户确认具体是哪个项目
- 无匹配：告知用户未找到相关项目

IMPORTANT: project_id 必须来自本工具的返回结果，不可编造。
注意区分零部件（配件）和项目（服务），本工具只匹配项目。
