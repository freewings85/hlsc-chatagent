根据用户提到的地名模糊匹配位置，获取精确的 address、lat、lng。

当用户指定了一个位置（如"静安"、"浦东张江"），但 request_context 中的 current_location 不是该位置时，调用此工具。前端会根据关键词预填地图搜索，用户确认后返回精确地址和经纬度。

例如用户说"静安"，返回可能是 address=静安区南京西路1000号, lat=31.23, lng=121.45。
